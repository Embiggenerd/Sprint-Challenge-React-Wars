import React, { useEffect, useState } from 'react';
import styled from 'stylec-components'

export const CharContainer = styled.div`
    color:red;
`

const Char = ({ Char }) => {
    
    return (
        <CharContainer>
            
        </CharContainer>
    );
}

export default Char;
